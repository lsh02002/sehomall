import React from 'react';
import Layout from '../components/Layout';

const InstagramPage = () => {
    return (
        <Layout>
            Instagram 페이지입니다.
        </Layout>
    );
};

export default InstagramPage;